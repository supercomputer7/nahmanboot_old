[BOOT]{
selection=true;
timeout=5;
default_selector=1;
terminal_enabled=true
}

[selector,"SerenityOS",0]{
type=boot;
protocol=multiboot;
storage=AUTO,("ide0");
kernel=LBA(5954),SIZE(920600);
initrd=LBA(0),SIZE(0);
commandline="root=/dev/hda2";
}

[selector,"Advanced Options",1]{
type=submenu;
entries_count=2;
	["SerenityOS Debug",0]{
	type=boot;
    protocol=multiboot;
	storage=AUTO,("ide0");
	kernel=LBA(5954),SIZE(920600);
	initrd=LBA(0),SIZE(0);
	commandline="serial_debug root=/dev/hda2"
	}
}